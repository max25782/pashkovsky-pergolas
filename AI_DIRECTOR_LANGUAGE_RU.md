# 🌐 Мультиязычный AI Director

## ✅ Что сделано:

Агент теперь **автоматически определяет язык** сообщения и отвечает на том же языке!

---

## 🎯 Поддерживаемые языки:

- 🇬🇧 **English** — по умолчанию
- 🇮🇱 **עברית (Hebrew)** — определяется по еврейским символам
- 🇷🇺 **Русский (Russian)** — определяется по кириллице

---

## 📋 Что нужно сделать (2 минуты):

### 1️⃣ Обновить System Prompt в AWS Bedrock

1. Откройте **AWS Bedrock Console**
2. Перейдите в **Agents** → выберите ваш Agent
3. Нажмите **Edit**
4. В разделе **Instructions** скопируйте промпт из файла:
   
   **`AI_DIRECTOR_MULTILINGUAL_PROMPT.md`**

5. Нажмите **Save**
6. Подождите 1-2 минуты

---

### 2️⃣ Перезапустить сервер (если он запущен)

```bash
# Остановите сервер (Ctrl+C)
# Затем запустите снова:
cd apps/crm
npm run dev
```

---

## 🧪 Тестирование:

Откройте чат AI Director и попробуйте на разных языках:

### 🇬🇧 English:
```
How many open deals do we have?
```

**Ожидаемый ответ:**
```
You have 12 open deals:
- 5 in Negotiation stage
- 4 in Contract stage
- 3 in Production stage
```

---

### 🇮🇱 עברית:
```
כמה עסקאות פתוחות יש לנו?
```

**Ожидаемый ответ:**
```
יש לכם 12 עסקאות פתוחות:
- 5 בשלב משא ומתן
- 4 בשלב חוזה
- 3 בשלב ייצור
```

---

### 🇷🇺 Русский:
```
Сколько у нас открытых сделок?
```

**Ожидаемый ответ:**
```
У вас 12 открытых сделок:
- 5 на этапе переговоров
- 4 на этапе контракта
- 3 на этапе производства
```

---

## 🔍 Как это работает:

1. **Пользователь пишет сообщение** на любом языке
2. **Система определяет язык** по символам:
   - Еврейские символы → `he`
   - Кириллица → `ru`
   - Остальное → `en`
3. **Язык передаётся в Bedrock** через `sessionAttributes.user_language`
4. **Агент отвечает** на том же языке

---

## 📁 Изменённые файлы:

### ✅ `/apps/crm/app/api/ai-director/chat/route.ts`
- Добавлена функция `detectLanguage()`
- Язык передаётся в `sessionAttributes`

### ✅ AWS Bedrock System Prompt
- Добавлена инструкция отвечать на языке пользователя
- Используется переменная `$user_language$`

---

## 💡 Дополнительные языки:

Если нужно добавить больше языков (например, арабский, китайский), просто обновите функцию `detectLanguage()` в файле:

**`apps/crm/app/api/ai-director/chat/route.ts`**

```typescript
function detectLanguage(message: string): string {
  // Hebrew
  if (/[\u0590-\u05FF]/.test(message)) return 'he'
  
  // Russian
  if (/[\u0400-\u04FF]/.test(message)) return 'ru'
  
  // Arabic
  if (/[\u0600-\u06FF]/.test(message)) return 'ar'
  
  // Chinese
  if (/[\u4E00-\u9FFF]/.test(message)) return 'zh'
  
  // Default to English
  return 'en'
}
```

И обновите System Prompt в AWS Bedrock, добавив эти языки в инструкции.

---

## ✅ Готово!

После применения изменений агент будет **автоматически отвечать на языке пользователя**! 🎉

---

## 🔧 Если не работает:

1. **Проверьте, что System Prompt обновлён** в AWS Bedrock Console
2. **Перезапустите сервер**: `npm run dev` в `apps/crm`
3. **Обновите страницу чата** в браузере (F5)
4. **Попробуйте снова** написать на разных языках

---

## 📞 Нужна помощь?

Если агент не отвечает на правильном языке, покажите:

1. Ваше сообщение
2. Ответ агента
3. Логи из терминала с `npm run dev`

Я помогу разобраться! 🚀





